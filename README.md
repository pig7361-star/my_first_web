# 오늘의 한만디

간단한 정적 웹페이지입니다. 버튼 클릭 시 랜덤 한문장(한만디)을 표시하며, 다크/라이트 모드를 지원합니다. 테마는 `localStorage`에 저장되어 재방문 시 유지됩니다.

실행 방법:

- 브라우저에서 `index.html`을 열기
- 또는 개발용 서버 실행 (예: `live-server` 또는 VS Code Live Server 확장)

주의:
- `script.js`는 `DOMContentLoaded` 이벤트에서 초기화합니다. 별도 빌드가 필요하지 않은 정적 페이지입니다.
